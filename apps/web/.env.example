# 前端環境變數範例（issue 8.4 #39）。複製為 .env / .env.local 後調整；勿提交含密之檔。
# 所有變數需以 VITE_ 開頭才會注入前端（Vite 慣例）。

# 後端 REST 基底 URL（預設 http://localhost:3000）。部署時改為實際 API 網域。
VITE_API_BASE_URL=http://localhost:3000

# 收到 401 時是否自動導向後端 SSO 登入（GET /auth/login）。預設 true。
# 本機未啟動後端或除錯時可設 false，改以畫面提示處理。
VITE_AUTH_REDIRECT=true

# 是否允許以示範資料（seed）呈現。預設 false。
# 注意：API 失敗時不會靜默退回 seed；僅在此旗標為 true 的開發模式下，feature 才提供 seed 切換。
VITE_USE_SEED=false

# （選用）dev server proxy 目標。設定後，vite dev 會將 /api 轉發至此目標並去除 /api 前綴。
# 同源部署或想避開 CORS 時使用；預設留空＝不啟用 proxy（client 以 VITE_API_BASE_URL 絕對位址直連）。
# VITE_DEV_PROXY=http://localhost:3000
