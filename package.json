{
  "name": "workflow-management-system",
  "version": "0.1.0",
  "private": true,
  "description": "內部工作流程管理系統 — monorepo（API + Web）",
  "engines": { "node": ">=20" },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r --parallel dev"
  }
}
