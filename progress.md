"# 專案開發進度（progress.md）\n\n> 供自動化開發排程與後續人類複查掌握現況。每輪任務結束時更新。\n\n## 技術堆疊（定案於 1.1）\n- Monorepo：pnpm workspace（`apps/api` + `apps/web`）、Node 20、TypeScript。\n- 後端：NestJS 10。資料庫：PostgreSQL 16（docker-compose）。\n- ORM：**Prisma**（於 1.2 定案）。\n- 認證：Entra ID OIDC（於 1.3 定案）。\n- 授權：RBAC（於 1.4 定案）。\n- 前端：**Vite + React 18 + TypeScript**（apps/web）；測試 **vitest**（於 2.2 引入）。\n- CI：`.github/workflows/ci.yml` — pnpm install（`--frozen-lockfile=false`）→ `pnpm -r build` → `pnpm -r test`。\n\n## 各 issue 狀態\n| WBS | Issue | 狀態 | 備註 |\n|-----|-------|------|------|\n| 1.1 | #8 專案初始化與技術選型 | ✅ done | 骨架、CI、docker-compose |\n| 1.2 | #9 資料模型與 schema | ✅ done | Prisma schema(16 實體/31 FK)、init migration、seed、PrismaModule |\n| 1.3 | #10 Microsoft 365 SSO | ✅ done | Entra OIDC Auth Code flow、群組對角色、停用拒絕、LoginAudit 稽核、單元測試 |\n| 1.4 | #11 RBAC | ✅ done | 權限矩陣、@Roles/@Permissions Guard、AccessScopeService 可見範圍、單元測試 |\n| 2.1 | #12 流程引擎：定義/狀態機/推進 | ✅ done | 純引擎狀態機 + WorkflowService、17 項單元測試 |\n| 2.2 | #13 流程定義設計器 UI | ✅ 本輪完成 | apps/web 前端設計器：步驟/角色/表單/下一步/流程設定，localStorage 持久化，vitest 測試 |\n| 2.3~6.x | #14-#30 | 待辦 | 依 WBS 順序；下一個為 #14（2.3 表單與產出文件管理），相依 2.2 已滿足 |\n\n## 2.2 交付物（本輪）\n- `apps/web/src/features/workflow-designer/`：前端「流程定義設計器」功能模組。\n  - `constants.ts`：FlowType / RoleCode / DeferStrategy 與顯示標籤，對齊 `apps/api/prisma/schema.prisma` 列舉。\n  - `types.ts`：草稿領域型別 `WorkflowDraft` / `StepDraft` / `StepFormRef` / `TriggerRule` / `CalendarRule`，對齊 WorkflowDefinition / StepDefinition / StepForm。\n  - `designer.ts`：**純邏輯**（無 React/DOM）——建立、`addStep`/`removeStep`/`moveStep`/`reindex`、表單增刪改、`nextStepOf`（下一步推導，與引擎一致以 order 升冪）、`validateWorkflow`（errors 阻擋儲存／warnings 提示）、`summarize`。\n  - `seed.ts`：依需求 §4–§7 產生四大標準流程種子（銷售／導入／環境建置／客製化），含各步驟角色、表單與 ISO 對應。\n  - `storage.ts`：`WorkflowRepository` 介面 + `localStorageRepository`（list/get/save/remove，首次自動載入種子）。\n  - `StepCard.tsx` / `WorkflowEditor.tsx` / `WorkflowDesigner.tsx`：左側流程清單 + 右側編輯器。可新增/編輯/移除/排序步驟、設定負責角色、必填表單與 ISO 對應、流程設定（觸發角色＋條件、行事曆遞延規則），每步顯示「完成後→下一步（負責人）」，並可儲存／刪除。\n  - `designer.test.ts`：vitest 單元測試（reindex/add/remove/move/nextStepOf/forms/validate/seed）。\n- `apps/web/src/App.tsx`：加入分頁導覽（流程定義設計器 / 系統狀態）。\n- `apps/web/package.json`：新增 `vitest` devDependency，`test` 指令改為 `vitest run`。\n\n## 技術決策（本輪，供 review）\n- **決策**：設計器邏輯與 UI 分離——`designer.ts` 為純函式（不可變更新），元件只呼叫並渲染。\n  **理由**：與既有後端純邏輯測試風格一致，可不依賴瀏覽器/DOM 完整測試流程編輯規則。\n- **決策**：本輪以 **localStorage** 持久化，封裝於 `WorkflowRepository` 介面。\n  **理由**：後端 WorkflowDefinition CRUD API 尚未建立（屬後續任務）；以 Repository 介面隔離，待後端就緒時改 REST 實作即可，不動 UI。驗收要點「可視覺化編輯流程並儲存、套用於新案件」以此達成（儲存的定義即新案件流程來源）。\n- **決策**：下一步以 order 升冪的相鄰步驟推導（與 2.1 引擎一致），不引入分支/條件式 next。\n  **理由**：schema 的 `StepDefinition.nextStepId` 目前為線性串接；條件分支（如買斷/訂閱）屬後續強化，先保留線性語意。\n- **決策**：UI 採內聯樣式（無 Tailwind/UI 套件），僅新增 vitest 一個 devDep。\n  **理由**：維持前端依賴極簡，降低 CI 風險（CI 以 `--frozen-lockfile=false` 安裝，新增 devDep 安全）。\n\n## 開發者需知\n- 本機/CI 驗證：本輪 `tsc --noEmit` 嚴格模式 0 錯誤；設計器純邏輯以 Node 原生 test runner 跑過 8/8（沙箱 esbuild 不可用，CI 的 Ubuntu 會正常跑 `vitest run`）。\n- **與後端銜接（下一步候選）**：待 WorkflowDefinition/StepDefinition CRUD API 完成後，新增 `restRepository` 實作 `WorkflowRepository` 並替換 `WorkflowDesigner` 注入即可；草稿型別欄位已對齊 schema，轉換成本低。\n- 條件分支流程（買斷 vs 訂閱）目前以「選擇性步驟（isOptional）」表達，尚未做條件式 next；屬後續強化。\n- 2.3（#14 表單與產出文件管理）：設計器已能標記每步「必填表單 + ISO 對應」，可作為 2.3 表單定義/必填把關（advance 前檢查）的前端來源。\n\n## 待釐清（沿用，需求 §12）\n- §12-1 跨角色移交是否需主管核可、流程一律由特定角色發起 → 設計器已留「觸發角色＋條件」欄位，實際核可關卡待釐清。\n- §12-5 行事曆遞延規則（順延下一工作日／整體後推）→ 設計器已留 deferStrategy 選項，實際曆法來源待 4.1 實作。\n"